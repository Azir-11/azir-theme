/**
 * 激活扩展
 * @param {object} context - VSCode ExtensionContext
 */
async function activate(context) {
  // 扩展激活成功
  console.log('Azir Theme extension is now active!')
}

module.exports = {
  activate,
}
